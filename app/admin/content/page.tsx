import Link from "next/link";
import { requireAdmin } from "@/lib/auth";
import { redirect } from "next/navigation";
export default async function ContentPage() { if (!(await requireAdmin())) redirect("/admin"); return <main className="admin-content"><div className="container"><div className="eyebrow">Content management</div><h1 style={{fontSize:56}}>Your story, editable.</h1><p style={{maxWidth:620}}>Games, promotions, events, gallery items and pages are stored in PostgreSQL. Protected CRUD endpoints are available at <strong>/api/admin/games</strong>, <strong>/api/admin/promotions</strong>, <strong>/api/admin/events</strong>, <strong>/api/admin/gallery</strong> and <strong>/api/admin/pages</strong>; use the upload endpoint for media.</p><div style={{display:"flex",gap:12,marginTop:30,flexWrap:"wrap"}}><Link className="button button-red" href="/admin/settings">Manage settings</Link><Link className="button button-outline-dark" href="/admin/messages">Manage messages</Link></div></div></main>; }