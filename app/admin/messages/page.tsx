import { prisma } from "@/lib/db";
import { deleteMessage, markMessage } from "../actions";
import { requireAdmin } from "@/lib/auth";
import { redirect } from "next/navigation";
export const dynamic = "force-dynamic";
export default async function MessagesPage() { if (!(await requireAdmin())) redirect("/admin"); const messages = await prisma.contactMessage.findMany({orderBy:{createdAt:"desc"}}); return <main className="admin-content"><div className="container"><div className="eyebrow">Inbox</div><h1 style={{fontSize:56}}>Contact messages.</h1><table className="admin-table"><thead><tr><th>Name</th><th>Email</th><th>Received</th><th>Status</th><th>Actions</th></tr></thead><tbody>{messages.map(message=><tr key={message.id}><td><strong>{message.name}</strong><br/>{message.subject}</td><td>{message.email}</td><td>{message.createdAt.toLocaleDateString()}</td><td>{message.read ? "Read" : "Unread"}</td><td><details><summary>View</summary><p>{message.message}</p><form action={markMessage}><input type="hidden" name="id" value={message.id}/><input type="hidden" name="read" value={String(!message.read)}/><button className="button button-dark" type="submit">Mark {message.read ? "unread" : "read"}</button></form><form action={deleteMessage}><input type="hidden" name="id" value={message.id}/><button className="button button-outline-dark" type="submit">Delete</button></form></details></td></tr>)}</tbody></table></div></main>; }