import { login } from "./actions";
export default function AdminLogin({ searchParams }: { searchParams: Promise<{error?:string}> }) { return <main className="admin-shell"><div className="container" style={{padding:"100px 0",maxWidth:520}}><div className="logo" style={{marginBottom:35}}>86 <span>CASINO</span></div><h1 style={{fontSize:48}}>Admin sign in</h1><p>Manage the content and conversations at 86.</p><form className="admin-form" action={login}><label>Email<input required type="email" name="email" autoComplete="username"/></label><label>Password<input required type="password" name="password" autoComplete="current-password"/></label><button className="button button-red" type="submit">Sign in</button><small>Credentials are configured through Railway environment variables.</small></form></div></main>; }