"use client";
import { useState } from "react";
export function ContactForm() { const [sent, setSent] = useState(false); const [error, setError] = useState(""); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setError(""); const response = await fetch("/api/contact", { method:"POST", body:JSON.stringify(Object.fromEntries(new FormData(event.currentTarget))), headers:{"Content-Type":"application/json"} }); if (response.ok) setSent(true); else setError("Please check your details and try again."); } if (sent) return <p>Thank you. Your message has been received and our team will be in touch.</p>; return <form className="contact-form" onSubmit={submit}><input required name="name" placeholder="Name"/><input required type="email" name="email" placeholder="Email address"/><input name="phone" placeholder="Phone number"/><input name="subject" placeholder="Subject"/><textarea required name="message" placeholder="How can we help?"/><button className="button button-red" type="submit">Send message</button>{error && <p style={{color:"var(--red)"}}>{error}</p>}</form>; }