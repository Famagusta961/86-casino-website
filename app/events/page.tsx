import Image from "next/image";
import { events } from "@/lib/content";
export const dynamic = "force-dynamic";
export default async function EventsPage() { const items = await events(); return <main><section className="page-hero honeycomb-dark"><div className="container"><div className="eyebrow light">Events & entertainment</div><h1>Stay for<br/>the atmosphere.</h1><p>Live music, good company and an ever-changing reason to come back.</p></div></section><section className="section light honeycomb"><div className="container"><div className="event-grid">{items.map(item => <article className="event-card" key={item.id}><Image src={item.image} alt={item.title} width={700} height={440}/><div className="event-card-body"><div className="event-date">{new Date(item.dateTime).toLocaleDateString("en-GH", { weekday:"long", month:"long", day:"numeric" })}</div><h3>{item.title}</h3><div className="eyebrow">{item.subtitle}</div><p>{item.description}</p></div></article>)}</div></div></section></main>; }